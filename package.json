{
  "name": "walnut",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test --since",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  },
  "devDependencies": {
    "cross-env": "^7.0.2",
    "cross-var": "^1.1.0",
    "lerna": "^3.22.1"
  }
}
